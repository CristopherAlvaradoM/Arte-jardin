---
import { getCollection } from "astro:content";
import Navbar from "../components/Navbar.astro";
import Layout from "../layouts/Layout.astro";
import ServiciosCard from "../components/ServiciosCard.astro";

const serviciosCardsInfo = await getCollection("serviciosCards");
---

<Layout>
  <Navbar />

  <header
    class="relative h-screen flex flex-col items-center justify-center text-white"
  >
    <!-- Imagen de fondo -->
    <div class="absolute inset-0">
      <img
        src="/index.jpeg"
        alt="Fondo"
        class="w-full h-full object-cover brightness-50 pointer-events-none"
      />
    </div>

    <!-- Contenido centrado -->
    <div class="relative z-10 text-center">
      <h1 class="text-8xl font-medium font-secondary p-16">Arte Jardín</h1>
      <p class="text-4xl mt-4 max-w-4xl">
        Espacios vivos que trascienden lo ordinario: <br />arte, naturaleza y
        armonía en cada rincón.
      </p>
    </div>
  </header>
  <main>
    <section
      class="flex mx-10 flex-col h-full md:flex-row items-center justify-center bg-white min-h-screen p-8"
    >
      <!-- Contenedor de texto -->
      <div
        class="md:w-1/2 text-2xl flex flex-col gap-10 font-medium text-center md:text-left"
      >
        <h2
          class="text-5xl font-medium text-green-900 font-secondary md:self-start w-full"
        >
          ¿Quiénes somos?
        </h2>
        <p class="text-gray-700 mt-4">
          Transformamos espacios en jardines artísticos y sostenibles que
          celebran la biodiversidad. En Arte Jardín fusionamos paisajismo,
          ecología y diseño para crear entornos únicos que respetan la
          naturaleza y reflejan tu personalidad. Descubre cómo podemos
          transformar tu espacio.
        </p>
      </div>
      <!-- Contenedor de imagen -->
      <div class="md:w-1/2 flex w-full h-full">
        <img
          src="inicioo.jpg"
          alt="Jardín moderno"
          class="rounded-lg w-full h-full object-cover"
        />
      </div>
    </section>
    <section class="pb-10">
      <hr class="border-t-2 border-black my-4 mx-10" />
      <h2
        class="font-secondary font-medium text-green-900 text-5xl self-center place-self-center my-10"
      >
        Nuestros servicios
      </h2>
      <p
        class="font-normal text-xl place-self-center self-center text-center pb-10 mx-24"
      >
        Cada jardín es diseñado considerando las condiciones específicas del
        sitio y las preferencias estéticas del cliente, creando espacios verdes
        únicos que celebran la belleza.
      </p>
      <ul class="flex flex-wrap justify-center gap-x-14 gap-y-14">
        {
          serviciosCardsInfo.map((infoCard) => {
            const { titulo, imagen, alt } = infoCard.data;
            const { body: descripcion } = infoCard;
            return (
              <ServiciosCard
                titulo={titulo}
                descripcion={descripcion}
                imagen={imagen}
                alt={alt}
              />
            );
          })
        }
      </ul>
    </section>
    <section>
      <hr class="border-t-2 border-black my-4 mx-10" />
      <h2
        class="font-secondary font-medium text-green-900 text-5xl self-center place-self-center my-10"
      >
        Proyectos
      </h2>
      <p
        class="font-normal text-xl place-self-center self-center text-center pb-10 mx-24"
      >
      Explore nuestra galería de jardines donde el arte y la naturaleza coexisten en perfecta armonía. Cada proyecto demuestra cómo las especies nativas, los elementos artísticos y el diseño sostenible pueden crear espacios extraordinarios.
      </p>
      <ul class="flex flex-wrap justify-center gap-x-14 gap-y-14">
        <li class="bg-white max-w-96 flex flex-col h-[450px]">
          <!-- Imagen ocupa el 50% -->
          <img
            src="/proyecto1.jpg"
            alt="Proyecto 1"
            class="w-full h-1/2 object-cover"
            loading="lazy"
          />

          <!-- Contenido ocupa el otro 50% -->
          <div class="h-1/2 p-4 flex flex-col justify-between border-b border-l border-r border-gray-900">
            <!-- Bloque del título -->
            <div class="h-[60px] flex items-center justify-center">
              <h3 class="text-lg font-semibold text-center leading-snug">
                Proyecto 1
              </h3>
            </div>

            <!-- Bloque del párrafo -->
            <div class="h-[140px] flex items-center justify-center">
              <p class="text-sm text-gray-600 text-center line-clamp-8 leading-snug">
                Descripción del proyecto 1.
              </p>
            </div>
          </div>
        </li>
      </ul>
    </section>
  </main>
</Layout>
