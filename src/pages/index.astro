---
import { getCollection } from "astro:content";
import Navbar from "../components/Navbar.astro";
import Layout from "../layouts/Layout.astro";
import ServiciosCard from "../components/ServiciosCard.astro";
import Footer from "../components/Footer.astro";

const serviciosCardsInfo = await getCollection("serviciosCards");
---

<Layout>
  <Navbar />

  <header
    class="relative h-screen flex flex-col items-center justify-center text-white"
  >
    <!-- Imagen de fondo -->
    <div class="absolute inset-0">
      <img
        src="/index.jpeg"
        alt="Fondo"
        class="w-full h-full object-cover brightness-50 pointer-events-none"
      />
    </div>

    <!-- Contenido centrado -->
    <div class="relative z-10 text-center">
      <h1 class="text-8xl font-medium font-secondary p-16">Arte Jardín</h1>
      <p class="text-4xl mt-4 max-w-4xl">
        Espacios vivos que trascienden lo ordinario: <br />arte, naturaleza y
        armonía en cada rincón.
      </p>
    </div>
  </header>
  <main>
    <section
      class="flex mx-10 flex-col h-full md:flex-row items-center justify-center bg-white min-h-screen p-8"
    >
      <!-- Contenedor de texto -->
      <div
        class="md:w-1/2 text-2xl flex flex-col gap-10 font-medium text-center md:text-left"
      >
        <h2
          class="text-5xl font-medium text-green_primary font-secondary md:self-start w-full"
        >
          ¿Quiénes somos?
        </h2>
        <p class="text-gray-700 mt-4">
          Transformamos espacios en jardines artísticos y sostenibles que
          celebran la biodiversidad. En Arte Jardín fusionamos paisajismo,
          ecología y diseño para crear entornos únicos que respetan la
          naturaleza y reflejan tu personalidad. Descubre cómo podemos
          transformar tu espacio.
        </p>
      </div>
      <!-- Contenedor de imagen -->
      <div class="md:w-1/2 flex w-full h-full">
        <img
          src="inicioo.jpg"
          alt="Jardín moderno"
          class="rounded-lg w-full h-full object-cover"
        />
      </div>
    </section>
    <section class="pb-10">
      <hr class="border-t-2 border-black my-4 mx-10" />
      <h2
        class="font-secondary font-medium text-green_primary text-5xl self-center place-self-center my-10"
      >
        Nuestros servicios
      </h2>
      <p
        class="text-2xl font-medium text-gray-700 place-self-center self-center text-center pb-10 mx-24"
      >
        Cada jardín es diseñado considerando las condiciones específicas del
        sitio y las preferencias estéticas del cliente, creando espacios verdes
        únicos que celebran la belleza.
      </p>
      <ul class="flex flex-wrap justify-center gap-x-14 gap-y-14">
        {
          serviciosCardsInfo.map((infoCard) => {
            const { titulo, imagen, alt } = infoCard.data;
            const { body: descripcion } = infoCard;
            return (
              <ServiciosCard
                titulo={titulo}
                descripcion={descripcion}
                imagen={imagen}
                alt={alt}
              />
            );
          })
        }
      </ul>
    </section>
    <section>
      <hr class="border-t-2 border-black my-4 mx-10" />
      <h2
        class="font-secondary font-medium text-green_primary text-5xl self-center place-self-center my-10"
      >
        Proyectos
      </h2>
      <p
        class="text-2xl font-medium text-gray-700 place-self-center self-center text-center pb-10 mx-24"
      >
        Explore nuestra galería de jardines donde el arte y la naturaleza
        coexisten en perfecta armonía. Cada proyecto demuestra cómo las especies
        nativas, los elementos artísticos y el diseño sostenible pueden crear
        espacios extraordinarios.
      </p>
      <ul class="flex flex-wrap justify-center gap-x-14 gap-y-14">
        <li>
          <div class="flex flex-col">
            <img src="nosotros1.png" alt="" class="w-[640px] h-[440px] object-cover" />
            <div class="flex flex-row content-center items-center gap-5 mt-2">
              <h3 class="text-3xl font-medium font-secondary text-green_tertiary">Nombre del proyecto</h3>
              <img src="arrow-right.png" alt="Siguiente"/>
            </div>
          </div>
        </li>
        <li>
          <div class="flex flex-col">
            <img src="nosotros1.png" alt="" class="lg:w-[640px]  lg:h-[440px] object-cover" />
            <div class="flex flex-row content-center items-center gap-5 mt-2">
              <h3 class="text-3xl font-medium font-secondary text-green_tertiary">Nombre del proyecto</h3>
              <img src="arrow-right.png" class="transition-transform duration-300 ease-in-out hover:translate-x-2" alt="Siguiente"/>
            </div>
          </div>
        </li>
      </ul>
    </section>
  </main>
  <Footer/>
</Layout>
