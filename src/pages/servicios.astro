---
import { getCollection } from "astro:content";
import Navbar from "../components/Navbar.astro";
import Layout from "../layouts/Layout.astro";
import ServiciosCardLeft from "../components/ServiciosCardLeft.astro";

const serviciosCardsInfo = await getCollection("servicios");
---

<Layout>
  <Navbar />

  <header
    class="relative py-40 flex flex-col items-center justify-center text-white"
  >
    <div class="absolute inset-0">
      <img
        src="/inicioServicio.png"
        alt="Una roca"
        class="w-full h-full object-cover brightness-50 pointer-events-none"
      />
    </div>

    <div class="relative z-10 text-center px-4">
      <h2 class="text-3xl md:text-6xl font-medium font-secondary mb-6 md:p-10">
        Nuestros servicios
      </h2>
      <p class="text-lg md:text-3xl max-w-[1080px]">
        Desde césped en rollo hasta jardines naturalistas y resilientes, diseñamos paisajes que 
        fusionan biodiversidad, sostenibilidad y arte.
      </p>
    </div>
  </header>

  <main class="py-16 flex flex-col items-center gap-16 px-10">
    {
      serviciosCardsInfo.map((infoCard) => {
        const { titulo, imagen, alt } = infoCard.data;
        const { body: descripcion } = infoCard;
        return (
          <ServiciosCardLeft
            titulo={titulo}
            descripcion={descripcion}
            imagen={imagen}
            alt={alt}
          />
        );
      })
    }

    <!-- Contactacto -->
    <div class="relative w-full h-auto">
      <img
        src="contactanos.png"
        alt="Fondo"
        class="absolute inset-0 w-full h-full object-cover z-0"
      />

      <!-- Capa oscura para mejorar legibilidad del texto -->
      <div class="absolute inset-0 bg-black bg-opacity-50 z-10"></div>

      <!-- Contenido centrado -->
      <div
        class="relative z-20 flex flex-col items-center justify-center text-white text-center py-36 px-6 md:px-32"
      >
        <h2 class="text-3xl md:text-5xl font-bold mb-4">Contáctanos</h2>
        <p class="text-sm md:text-2xl mb-6 py-3">
          ¿Te gustaría transformar tus espacios en un santuario sostenible y
          hermoso?<br />
          Contáctanos para crear un jardín que te conecte con la naturaleza.
        </p>
        <a
          href="/contacto"
          class="bg-green_tertiary hover:bg-green-700 text-white text-lg md:text-2xl font-medium px-10 md:px-28 py-3 rounded-full transition-transform duration-300 ease-in-out hover:scale-105"
        >
          Contáctanos
        </a>
      </div>
    </div>
  </main>
</Layout>